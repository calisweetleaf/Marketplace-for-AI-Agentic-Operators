# Skills Projection Note

This folder exists as a CTMv3/Codex topology signal only.

Do not copy package skills here. The canonical Aeriadne skill payloads for this staging workspace live in the workspace root `skills/` directory so Codex, Claude Code, OpenCode, registry cards, and marketplace docs all point at one source.
