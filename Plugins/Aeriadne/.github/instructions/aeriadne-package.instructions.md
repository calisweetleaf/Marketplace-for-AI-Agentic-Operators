# Aeriadne Package Instructions

Apply to Aeriadne package files, registry files, marketplace cards, adapters, agent prompts, MCP/server cards, and validation scripts.

Use `registry/` as machine-readable package truth and `marketplace/` as the human-facing projection. Keep client-specific assumptions in `adapters/`. Keep canonical server planes external unless a package is intentionally vendored and approved.
